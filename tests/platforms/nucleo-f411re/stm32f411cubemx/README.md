# stm32f411cubemx/README

This folder is generated by STM32CubeMX.

The `main()` function is renamed via a define, to avoid clashing with
µOS++ similar function. Its content is manually copied to
`os_startup_initialize_hardware()`.
